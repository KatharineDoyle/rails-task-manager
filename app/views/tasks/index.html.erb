<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <% if task.completed == true %>
      <%= "[x]" %>
      <p> <%= link_to task.title, task_path(task[:id]) %></p>
      <p><%= link_to "🗑", destroy_task_path(task[:id]), method: :delete %></p>

      <% else %>
      <%= "[ ]" %>
       <p> <%= link_to task.title, task_path(task[:id]) %></p>
      <p><%= link_to "🗑", destroy_task_path(task[:id]), method: :delete %></p>
      <% end %>
  </li>
  <% end %>
  <%= link_to "+ Add a new task", tasks_new_path %>
</ul>

