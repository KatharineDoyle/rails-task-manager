<h1><%= @task.title %></h1>
<p><%= @task.details %>
<% if @task.completed == true %>
      <%= "[x]" %>
      <% else %>
      <%= "[ ]" %>
      <% end %>
</p>

 <%= link_to "Edit task", edit_task_path(@task) %>
 <%= link_to "Destroy task", destroy_task_path(@task), method: :delete %>
